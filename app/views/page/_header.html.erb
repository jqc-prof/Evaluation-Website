<nav class="navbar navbar-dark navbar-expand-lg ">
    <div class="container-fluid">
        <%= link_to 'Pg7FinalProject', root_path, class:"navbar-brand" %>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <%= link_to "Assignments", assignments_path, class:"nav-link"  %>
                </li>

                <li class="nav-item">
                    <%= link_to "Submissions", submissions_path, class:"nav-link"  %>
                </li>

                <li class="nav-item">
                    <%= link_to "Ratings", ratings_path, class:"nav-link"  %>
                </li>
            </ul>
            <% if current_user%>
                <button type="button" class="me-3 btn btn-outline-light">
                    <%=current_user.role%>
                    <span class="badge text-dark bg-light">
                        <%=current_user.email.split('@')[0]%>
                    </span>
                </button>
            <% end %>
            <div class="d-flex btn-group me-3" style="">
                <% if current_user%>

                <button class="d-flex btn btn-outline-warning" type="submit">
                    <%= link_to 'Sign out', destroy_user_session_path, method: :delete%>
                </button>
                <%else%>
                    <button class="btn btn-outline-primary" type="submit">
                        <%= link_to 'Sign in', new_user_session_path %>
                    </button>

                    <button class="btn btn-outline-primary" type="submit">
                        <%= link_to 'Sign up', new_user_registration_path %>
                    </button>
                <% end %>
            </div>
            <%=render 'search/form'%>

            </form>
        </div>
    </div>
</nav>